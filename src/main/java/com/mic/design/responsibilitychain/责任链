1.概念
    是一个请求有多个对象来处理，这些对象是一条链，但具体由哪个对象来处理，根据条件判断来确定，如果当前对象不能
    处理会传递给该链中的下一个对象，直到有对象处理它为止，如android中的事件分发机制,异常处理，有序广播

2.使用场景
    2.1 有多个对象可以处理同一个请求，具体哪个对象处理该请求待运行时刻再确定
    2.2 在不明确指定接收者的情况下，向多个对象中的一个提交一个请求
    2.3 可动态指定一组对象处理请求，客户端可以动态创建职责链来处理请求

3.ViewGroup/view事件传递
    3.1 主要概念
       1.MotionEvent  dwon up move
       2.dispatchTouchEvent 返回true 表示当前view消耗了事件 ,不会向下传递
    3.2 viewGroup的事件分发
       1.onInterreptTouchEvent true表示拦截了事件，不会向下传递了
    3.3 view的事件分发