package com.mic.p2_function

/**
 * Kotlin中的判等性主要有两种类型：
 * 1·结构相等(值相等)
 * 通过操作符==来判断两个对象的内容是否相等。
 *
 * 2·引用相等。
 * 通过操作符===来判断两个对象的引用是否一样，与之相反的判断操作符是！==。
 * 如果比较的是在运行时的原始类型，比如Int，那么===判断的效果也等价于==。
 */
